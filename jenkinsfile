pipeline {
    agent any
    
tools {
    maven 'Maven'
  }  
  
environment {

    JAVA_VERSION = "${params.JAVA_VERSION}"
}

stages {

 stage ('Build Docker Image'){
        steps {
          sh '''
          docker build -t syed:java8 .
          '''
        }
      }
     } 
}
